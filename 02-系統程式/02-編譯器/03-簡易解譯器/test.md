
```
(env) cccimac@cccimacdeiMac 04-簡易解譯器 % ./test.sh
Simple Basic-like Interpreter with GOTO and IF
Enter program lines (empty line to run, 'quit' to exit):
> x=3
> loop:
> print x
> x=x-1
> if x>0 goto loop
> 
3
2
1

Enter new program:
```